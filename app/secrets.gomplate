{
    "foo": {{ (datasource "secrets").foo | data.ToJSONPretty "  "}},
    "baz": {{ (datasource "secrets").baz }},
    "qux": {{ (datasource "secrets").qux | data.ToJSONPretty "  "}},
    "quux": {{ (datasource "secrets").quux }},
    "grault": {{ (datasource "secrets").grault }},
    "garply": {{ (datasource "secrets").garply | data.ToJSONPretty "  " }},
    "corge": {{ (datasource "secrets").corge  | data.ToJSONPretty "  " }}
}
