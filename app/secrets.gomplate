{
    "foo": {{ (ds "secrets").foo | data.ToJSONPretty "  "}},
    "baz": {{ (ds "secrets").baz }},
    "qux": {{ (ds "secrets").qux | data.ToJSONPretty "  "}},
    "quux": {{ (ds "secrets").quux }},
    "grault": {{ (ds "secrets").grault }},
    "garply": {{ (ds "secrets").garply | data.ToJSONPretty "  " }},
    "corge": {{ (ds "secrets").corge  | data.ToJSONPretty "  " }}
}
